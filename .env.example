# Example environment file
# Use a relative path in development to hit the Vite proxy (avoids CORS)
VITE_API_BASE=/RRService

# For production you can override to the full remote base URL if needed
# VITE_API_BASE=https://radar.Giftologygroup.com/RRService
